def twoSum(self, nums: List[int], target: int) -> List[int]:
    j=-1
    for i in range(1,len(nums):
        temp=[: i]
        if (targets-nums[i]) in temp:
            j=temp.index(targets-nums[i])
            break
    if j>=0:
        return [j,i]

              
